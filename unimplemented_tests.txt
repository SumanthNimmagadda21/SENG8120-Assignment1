- Decimal input handling (e.g., 2.5, 3.0) — Bash doesn’t support floats directly.
- Overflow test: very large numbers beyond shell arithmetic limits.
- Custom error message formatting per invalid input type.
- Integration test with user input via `read`.
- Test with file-based input instead of CLI args.